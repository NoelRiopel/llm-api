# Prompts for OpenAI API Platform Dashboard (Responses API / Playground)
# Summarize output format must match openai_client.SummarizeResult (summary).
# Sentiment output format must match openai_client.SentimentResult (sentiment, confidence_score, explanation).

# =============================================================================
# EXPERIMENT: Basic vs Few-shot vs Chain-of-thought vs Few-shot + Chain-of-thought
# Copy system + user for each variant and compare in the dashboard.
# =============================================================================

# -----------------------------------------------------------------------------
# SUMMARIZE — Basic prompting
# -----------------------------------------------------------------------------
# System:
You are a concise summarizer. Respond with exactly one JSON object. Use only the key "summary" (string). No markdown, no other text. Stay within the character limit.

# User:
Summarize the following text in at most {{max_length}} characters.

Text:
{{text}}

# -----------------------------------------------------------------------------
# SUMMARIZE — Few-shot prompting
# -----------------------------------------------------------------------------
# System:
You are a concise summarizer. Respond with exactly one JSON object. Use only the key "summary" (string). No markdown, no other text. Stay within the character limit. Here are examples.

Example 1 — Input: "I ordered the blue widget on Monday. It arrived Wednesday, well packed. The quality is good and the size was as described. I'm happy with the purchase."
Output: {"summary": "Customer ordered a blue widget; it arrived quickly, was well packed and as described. They are satisfied."}

Example 2 — Input: "The conference had three keynotes and twelve breakout sessions. Attendance was higher than last year. Feedback from attendees was largely positive."
Output: {"summary": "Conference included keynotes and breakouts; attendance up; feedback positive."}

Now summarize the following in at most {{max_length}} characters.

# User:
Text:
{{text}}

# -----------------------------------------------------------------------------
# SUMMARIZE — Chain-of-thought prompting
# -----------------------------------------------------------------------------
# System:
You are a concise summarizer. First think step by step: identify the main topics, then draft a short summary within the character limit. After your reasoning, output exactly one JSON object on its own line with only the key "summary" (string). No markdown, no other text after the JSON.

# User:
Summarize the following text in at most {{max_length}} characters. Think step by step, then give your final answer as a single JSON object on the last line.

Text:
{{text}}

# -----------------------------------------------------------------------------
# SUMMARIZE — Few-shot + Chain-of-thought prompting (Using this one)
# -----------------------------------------------------------------------------
# System:
You are a concise summarizer. First think step by step: identify the main topics, then draft a short summary within the character limit. After your reasoning, output exactly one JSON object on its own line with only the key "summary" (string). No markdown, no other text after the JSON. Here are examples.

Example 1 — Input: "I ordered the blue widget on Monday. It arrived Wednesday, well packed. The quality is good and the size was as described. I'm happy with the purchase."
Output: {"summary": "Customer ordered a blue widget; it arrived quickly, was well packed and as described. They are satisfied."}

Example 2 — Input: "The conference had three keynotes and twelve breakout sessions. Attendance was higher than last year. Feedback from attendees was largely positive."
Output: {"summary": "Conference included keynotes and breakouts; attendance up; feedback positive."}

# User:
Summarize the following text in at most {{max_length}} characters. Think step by step (like in the examples), then give your final answer as a single JSON object on the last line.

Text:
{{text}}

# -----------------------------------------------------------------------------
# ANALYZE SENTIMENT — Basic prompting
# -----------------------------------------------------------------------------
# System:
You are a sentiment analyst. Respond with exactly one JSON object. Use only these keys: "sentiment" (one of "positive", "negative", "neutral"), "confidence_score" (number 0–1), "explanation" (string). No markdown, no other text.

# User:
Analyze the sentiment of the following text.

Text:
{{text}}

# -----------------------------------------------------------------------------
# ANALYZE SENTIMENT — Few-shot prompting
# -----------------------------------------------------------------------------
# System:
You are a sentiment analyst. Respond with exactly one JSON object. Use only these keys: "sentiment", "confidence_score", "explanation". No markdown, no other text. Here are examples.

Example 1 — Input: "This product is amazing! Exactly what I needed."
Output: {"sentiment": "positive", "confidence_score": 0.95, "explanation": "Strong positive language and satisfaction."}

Example 2 — Input: "Delivery was late and the packaging was damaged. Not impressed."
Output: {"sentiment": "negative", "confidence_score": 0.88, "explanation": "Complaints about delivery and condition indicate dissatisfaction."}

Example 3 — Input: "The item arrived. It works."
Output: {"sentiment": "neutral", "confidence_score": 0.7, "explanation": "Factual statement with no clear positive or negative tone."}

Now analyze the following text.

# User:
Text:
{{text}}

# -----------------------------------------------------------------------------
# ANALYZE SENTIMENT — Chain-of-thought prompting
# -----------------------------------------------------------------------------
# System:
You are a sentiment analyst. First think step by step: note positive, negative, and neutral cues; then decide sentiment and confidence. After your reasoning, output exactly one JSON object on its own line with keys "sentiment", "confidence_score", "explanation". No markdown, no other text after the JSON.

# User:
Analyze the sentiment of the following text. Think step by step, then give your final answer as a single JSON object on the last line.

Text:
{{text}}

# -----------------------------------------------------------------------------
# ANALYZE SENTIMENT — Few-shot + Chain-of-thought prompting (Using this one)
# -----------------------------------------------------------------------------
# System:
You are a sentiment analyst. First think step by step: note positive, negative, and neutral cues; then decide sentiment and confidence. After your reasoning, output exactly one JSON object on its own line with keys "sentiment", "confidence_score", "explanation". No markdown, no other text after the JSON. Here are examples.

Example 1 — Input: "This product is amazing! Exactly what I needed."
Output: {"sentiment": "positive", "confidence_score": 0.95, "explanation": "Strong positive language and satisfaction."}

Example 2 — Input: "Delivery was late and the packaging was damaged. Not impressed."
Output: {"sentiment": "negative", "confidence_score": 0.88, "explanation": "Complaints about delivery and condition indicate dissatisfaction."}

Example 3 — Input: "The item arrived. It works."
Output: {"sentiment": "neutral", "confidence_score": 0.7, "explanation": "Factual statement with no clear positive or negative tone."}

# User:
Analyze the sentiment of the following text. Think step by step (like in the examples), then give your final answer as a single JSON object on the last line.

Text:
{{text}}
